local StarterGui = game:GetService("StarterGui")
local link = "https://discord.gg/DNMCvwrdKG"

local callbackFunction = Instance.new("BindableFunction")
callbackFunction.OnInvoke = function(buttonPressed)
    if setclipboard then
        setclipboard(link)
    elseif toclipboard then
        toclipboard(link)
    elseif syn and syn.write_clipboard then
        syn.write_clipboard(link)
    else
        print("Copy this link :", link)
    end
end

StarterGui:SetCore("SendNotification", {
    Title = "AVATAR CHANGER",
    Text = "This Script IS PAID join our discord for PURCHASE",
    Duration = 5,
    Button1 = "COPY LINK",
    Callback = callbackFunction
})
StarterGui:SetCore("SendNotification", {
    Title = "AVATAR CHANGER",
    Text = "THE SCRIPT IS 3$ ALSO IF YOU JOIN DISCORD FREE 1 DAY TRIAL",
    Duration = 5,
})
